apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello
  namespace: dev
spec:
  ingressClassName: nginx
  rules:
    - host: hello.dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello
                port:
                  number: 80
